name: Deliver container

on: push

jobs:
  delivery:
    runs-on: ubuntu-latest
    name: Deliver container
    steps:
      - name: Check out repository
        uses: actions/checkout@v1

      - name: Docker Login
        run: docker login -u r0819654 -p Genz2001!

      - name: Docker Build
        run: docker build -t r0819654/herexamen-project-api-development:latest .

      - name: Upload container to Docker Hub with Push
        run: docker push r0819654/herexamen-project-api-development:latest